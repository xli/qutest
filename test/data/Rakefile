
$LOAD_PATH << File.join(File.dirname(__FILE__), '..', '..', 'lib')
require 'qutest'

task :broadcast do
  Qutest.kestrel_queue('queue_name') << "#{File.dir(__FILE__)}/*_test.rb"
end

task :qutest do
  Qutest.run(Qutest.kestrel_queue('queue_name'))
end
